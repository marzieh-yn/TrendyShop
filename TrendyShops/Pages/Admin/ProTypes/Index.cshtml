@page
@model TrendyShops.Pages.Admin.ProTypes.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout2.cshtml";
}

<section class="content">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12">
            
            <!-- /.card -->

            <div class="card">
              <div class="card-header">
                <h3 class="card-title">Product Types</h3>
              </div>
              <!-- /.card-header -->
              <div class="card-body">
                <table id="example1" class="table table-bordered table-striped">
                  <thead>
                  <tr>
                    <th>
                Product
            </th>
            <th>
                NewArrival
            </th>
            <th>
                ThreeItems
            </th>
             <th>
                BestSeller
            </th>
            <th>
               Status
            </th>
                    <th>Action</th>
                  </tr>
                  </thead>
                  <tbody>
                    @foreach (var item in Model.ProTypes) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Product.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewArrival)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThreeItems)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BestSeller)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
                  </tbody>
                  <tfoot>
                  <tr>
                   <th>
                Product
            </th>
            <th>
                NewArrival
            </th>
            <th>
                ThreeItems
            </th>
             <th>
                BestSeller
            </th>
            <th>
                Status
            </th>
                    <th>Action</th>
                  </tr>
                  </tfoot>
                </table>
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
          </div>
          <!-- /.col -->
        </div>
        <!-- /.row -->
      </div>
      <!-- /.container-fluid -->
    </section>
    <!-- /.content -->

<p>
    <a asp-page="Create" class="btn btn-primary">Create New</a>
</p>


